/*====================
       *
       * ライトボックス
       *
       ====================*/

export default () => {
  lightbox.option({
    alwaysShowNavOnTouchDevices: true,
    disableScrolling: true,
    positionFromTop: 100,
  });
};
